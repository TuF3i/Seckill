# 绑定所有网络接口，允许其他容器连接
bind 0.0.0.0
# 设置访问密码
requirepass root
# 设置从节点连接主节点的密码
masterauth root
# 开启AOF持久化，提高数据安全性[reference:2]
appendonly yes
# 设定内存上限及淘汰策略（根据实际情况调整）
maxmemory 256mb
maxmemory-policy allkeys-lru
